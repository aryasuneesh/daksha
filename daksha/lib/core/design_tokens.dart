import 'package:flutter/material.dart';

abstract final class DT {
  // Colors — Warm Desk palette
  static const bg         = Color(0xFFFAF7F2);
  static const elev1      = Color(0xFFF0EBE0);
  static const elev2      = Color(0xFFE5DECE);
  static const outline    = Color(0xFFCEC6B4);
  static const textStrong = Color(0xFF1F1B14);
  static const text       = Color(0xFF3D3729);
  static const muted      = Color(0xFF7A7259);
  static const primary    = Color(0xFF5C4A2E);
  static const primaryFg  = Color(0xFFFAF7F2);
  static const accent     = Color(0xFF8B5E2E);
  static const amber      = Color(0xFFC4860E);
  static const amberBg    = Color(0xFFFDF3DC);
  static const success    = Color(0xFF3F6B43);
  static const caution    = Color(0xFF9A6618);
  static const cautionBg  = Color(0xFFFEF3E2);
  static const error      = Color(0xFF8C2A1F);
  static const noteBg     = Color(0xFFFFFBF0);
  static const canvasBg   = Color(0xFF1C1814);

  // Spacing (4 px baseline grid)
  static const xs         = 4.0;
  static const sm         = 8.0;
  static const md         = 12.0;
  static const lg         = 16.0;
  static const cardHPad   = 18.0;
  static const contentPad = 20.0;
  static const btnHPad    = 22.0;
  static const bottomSafe = 28.0;

  // Radii
  static const radius       = 16.0;
  static const radiusBtn    = 999.0;
  static const radiusDialog = 20.0;

  // Border widths
  static const bwCard    = 1.0;
  static const bwCallout = 2.0;

  // Layout
  static const statusBarH = 44.0;
  static const topBarH    = 64.0;
  static const minTouch   = 44.0;
}
